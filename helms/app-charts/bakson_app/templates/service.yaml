apiVersion: v1
kind: Service
metadata:
  name: {{ include "bakson_app.fullname" . }}
  labels:
    {{- include "bakson_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bakson_app.selectorLabels" . | nindent 4 }}
